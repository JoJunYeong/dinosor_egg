// Standalone mode: Manus OAuth SDK replaced with stub
// This file is kept for import compatibility only

export type SessionPayload = {
  openId: string;
  appId: string;
  name: string;
};

class SDKStub {
  // No-op stubs for standalone deployment
}

export const sdk = new SDKStub();
